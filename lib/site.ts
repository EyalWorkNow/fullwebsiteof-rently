// Canonical production origin — used by metadataBase and the sitemap.
// TODO: confirm/replace once the production domain is final (terms + support
// emails already reference rently.co.il; no NEXT_PUBLIC_* origin var exists).
export const SITE_ORIGIN = 'https://rently.co.il'
